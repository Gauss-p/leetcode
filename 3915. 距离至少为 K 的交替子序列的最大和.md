### 题目分析
题目要求在给出的列表`nums`中找到一个子序列，满足以下要求：

>	1.相邻两个值之间的大小关系在“严格大于”和“严格小于”之间交替变换;
>	2.相邻两个位置在`nums`中的距离大于等于`k`.

并找到所有满足要求的子序列元素总和的最大值。

### 基本思路
#### DFS思路
看到子序列，第一时间即可想到暴力对所有可能的情况进行求解，因此可以想到用`dfs`函数对每一种可能的子序列进行计算，求出最大值即可。

定义`dfs(indx, cur)`用来计算：`nums[indx:]`中，在一定选择`nums[indx]`的情况下，可以达到的最大子序列元素总和，其中`cur`用来控制`nums[indx]`下一个值的选择：
> 	1.$cur=0$表示在子序列中，`nums[indx]`的下一个值需小于`nums[indx]`
> 	2.$cur=1$表示在子序列中，`nums[indx]`的下一个值需大于`nums[indx]`

这样，每次在`dfs`函数中即可根据$cur$的值来选择子序列中下一个值的位置`i`，只需满足以下条件即可：
$$i \in [indx+k, len(nums)),且\begin{cases} 
nums[indx]>nums[i] & if\ cur==0 \\
nums[indx]<nums[i] & if\ cur==1 \\
\end{cases}$$

因此可以很简单地写出上述`dfs`函数的代码如下，但由于其时间复杂度太高，无法通过：
```python
@cache
def dfs(indx, cur):
	if indx+k >= n:
		return nums[indx]
	ans = nums[indx]
	for i in range(indx+k, n):
		if (cur==1 and nums[indx]<nums[i]) or (cur==0 and nums[indx]>nums[i]):
			ans = max(ans, dfs(i, cur^1)+nums[indx])
	return ans
```

#### DP思路
由于上面`dfs`函数无法通过，因此可以想到将其转换为动态规划，尝试压缩。

动态规划思路大体相同，定义动态规划数组`dp`，其中`dp[i][cur]`表示在**前$i$个数字中**，以第`i`个数字为结尾选择子序列，能够满足条件的最大子序列元素和。`cur`的定义和上面一样，仍然是第`i`个数字的下一个数字和它的大小关系，因此，如果要根据`i`之前的位置的动态规划值来计算`i`对应的动态规划值，则需通过以下递推关系式：

$$\begin{cases} 
dp[i][0] = max(dp[j][1]+nums[i]) & if\ \ nums[j]<nums[i]\\
dp[i][1] = max(dp[j][0]+nums[i]) & if\ \ nums[j]>nums[i]\\
\end{cases}
\ \ \ \ \ (其中j \in [0,i-k])$$

类似的，可以写出以下动态规划代码，但时间复杂度太高，依然无法通过。

```python
n = len(nums)
dp = [[0,0] for _ in range(n)]
dp[0][0] = dp[0][1] = nums[0]
res = nums[0]
for i in range(1, n):
	dp[i][0] = dp[i][1] = nums[i]
	for j in range(i-k+1): # 循环找第i个元素的上一个
		if nums[j] < nums[i]:
			dp[i][0] = max(dp[i][0], dp[j][1]+nums[i])
		if nums[j] > nums[i]:
			dp[i][1] = max(dp[i][1], dp[j][0]+nums[i])
	res = max(res, dp[i][0], dp[i][1])
return res
```

#### FenwickTree优化
上述两个暴力方法都无法通过，因此不得不去想如何优化了。分析刚刚的解题过程，如果将递推关系式稍加变化，即可得到：

$$\begin{cases} 
dp[i][0] = max(dp[j][1])+nums[i] & if\dots\\
dp[i][1] = max(dp[j][0])+nums[i] & if\dots\\
\end{cases}$$

注意到$nums[i]$是不变的，因此为了求$dp[i]$，就需要求出所有满足条件的位置$j$，当然，更好的是能够直接求出对应的$dp[j][0]$和$dp[j][1]$的最大值。

观察两个条件，一个是$nums[j]<nums[i]$，一个是$nums[j]>nums[i]$，也就意味着所有满足条件的$j$其实可以根据`nums`中的值来确定：如果将`nums`中所有不同的值从小到大排序，并且能够找到`nums`中的值对应的位置，将这些位置对应的`dp`中的值按上述顺序排列起来，这样，直接在排列出来的所有值中求一个前缀或后缀最大值，即可快速求得需要的值。

但这里被排列的`dp`中的值不能是已经计算出来的所有`dp`值，为了满足题目中要求的「相邻两元素距离至少为`k`」，在计算位置`i`对应的`dp`值时，只能对位置在$[0,i-k]$之间的`dp`值进行排列，为了满足这个条件，在更新`dp`值存储的列表时，需要在计算完`i`之后再将`i-k+1`对应的`dp`值存入列表中，只有这样，在每一次新的循环开始时，才能保证列表中存储的所有`dp`值对应的位置都和当前位置距离至少为`k`。

分析上述算法，可以发现需要在一个值会变化的数组中求前缀最大值和后缀最大值，因此可以想到用$FenwickTree$算法进行优化，开两个$FenwickTree$，其中一个求前缀，一个求后缀，前者正常求即可，但后者需要将数组倒过来求前缀，这样即可快速求出所需答案。

### 实现细节
在维护两个$FenwickTree$数组时，对于那个倒过来计算前缀的数组，传入待更新位置时需要传倒过来的索引，而不能简单的用正序的索引进行处理。

### 复杂度
时间复杂度：$O(n\times logn)$
空间复杂度：$O(n)$

### 代码
```python
class FenwickTree:
    def __init__(self, n):
        self.n = n
        self.tree = [0]*(n+1)

    def update(self, index: int, val: int) -> None:
        while index <= self.n:
            self.tree[index] = max(self.tree[index], val)
            index += (index&(-index)) # 往后更新

    def preSum(self, pos):
        # 按照预期的方式求前缀最大值
        ans = 0
        while pos >= 1:
            ans = max(ans, self.tree[pos])
            pos -= (pos&(-pos))
        return ans

class Solution:
    def maxAlternatingSum(self, nums: list[int], k: int) -> int:
        stl = sorted(set(nums)) # 将nums中不同的数字进行排序
        rank = {v:i+1 for i,v in enumerate(stl)} # 将nums中的值快速转换成stl中的索引
        fwt0 = FenwickTree(len(stl))
        fwt1 = FenwickTree(len(stl))
        
        n = len(nums)
        dp = [[0,0] for _ in range(n)]
        res = nums[0]
        for i in range(n):
            dp[i][0] = dp[i][1] = nums[i]
            if i >= k:
                indx = rank[nums[i]] # 找到nums[i]在stl中的索引
                dp[i][1] = max(dp[i][1], fwt0.preSum(indx-1)+nums[i]) # indx-1即表示小于nums[i]的部分
                dp[i][0] = max(dp[i][0], fwt1.preSum(len(stl)-indx)+nums[i]) # len(stl)-indx即表示在倒序列表中大于nums[i]的部分

            if i-k+1 >= 0:
                indx = rank[nums[i-k+1]]
                fwt0.update(indx, dp[i-k+1][0]) # 在正序列表中更新i-k+1位置的值
                fwt1.update(len(stl)-indx+1, dp[i-k+1][1]) # 在倒序列表中更新i-k+1位置的值
                
            res = max(res, dp[i][0], dp[i][1]) # 更新答案

        return res
```