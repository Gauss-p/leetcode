### 题目分析
题目中对于一个物品$i$的免费物品$j$的要求如下：
1. $items[j][0]\%items[i][0]=0$，即$factor_j$是$factor_i$的倍数；
2. $j \neq i$；
3. 对于每一个购买的物品$i$，都只能享受单独一个免费的物品$j$，且所有物品$i$享受的免费物品不能重复。举个例子：当买了2个物品$i$时：
	1. 如果在数组中存在2个以上满足条件的物品$j$，那么每一个物品$i$就分别可以享受免费的$j_1$和$j_2$，此时总共可以获得4个物品；
	2. 如果数组中只有1个满足条件的物品$j$，那么两个物品$i$中只有一个可以享受免费的物品$j$，此时总共可以获得3个物品；
4. 购买不同的物品可以享受相同的免费物品。

### 基本思路
由题目分析可以发现，如果对于每一个$factor_i$都知道整个数组中满足$factor_j$是其倍数的下标$j$的数量，记为$cnt[factor_i]$，那么当购买$n$个物品$i$的时候，可以获得的免费物品数量就是：$min(cnt[factor_i], n)$，因此

### 实现细节

### 复杂度

### 代码
```python
class Solution:
    def maximumSaleItems(self, items: List[List[int]], budget: int) -> int:
        n = len(items)
        mx = max(item[0] for item in items)
        cnt = [0]*(mx+1)
        for item in items:
            cnt[item[0]] += 1
        
        multipleCnt = [0]+[sum(cnt[i::i])-1 for i in range(1, mx+1)]
        minPrice = min(item[1] for item in items)
        cheap = defaultdict(int)
        for fac, price in items:
            if multipleCnt[fac] and price < minPrice*2:
                cheap[price] += multipleCnt[fac]
        
        stl = sorted(cheap.items())
        res = 0
        for price, curNum in stl:
            canBuy = min(budget//price, curNum)
            res += canBuy*2
            budget -= canBuy*price
            if canBuy < curNum:
                break
        return res+(budget//minPrice)
```