### 题目分析
根据题目定义可知，固定点就是：在一个数组$lst$中满足$lst[i]=i$的下标$i$；同时，由于题目明确说明是在$nums$中删除任意数量元素，因此得到的实际上就是一个$nums$的子序列。

因此，题目要求的就是：$nums$的子序列中最大的「值和下标相等」的位置数量。

### 基本思路
#### DFS思路
分析中看到`子序列`，第一时间即可想到暴力求解，即用$dfs$的方式将所有可能的子序列都遍历一遍，然后对每一种情况都计算，求出最大的固定点数量即可。

定义`dfs(i, indx)`用来计算在前`i`个数字中，已经选择`indx`个数字时可以达到的最大固定点数量。那么对于第`i`个数字即$nums[i]$就有两种情况：

>	1.选择$nums[i]$作为第$indx$个数字，那么这时在下一次传入$dfs$函数的时候，新的$indx$值就需要加一，同时对于固定点数量的计算就有两种情况：
>	
>	>	$I: nums[i]=indx-1$ 也就是说在子序列中，$nums[i]$本身就是一个固定点，因此在后面计算出来的答案基础上需要加1才能得到当前的最大固定点数量；
>	>	$II: nums[i]\neq indx-1$ 也就是说$nums[i]$并不是一个固定点，而只是一个类似占位符的东西，因此后面计算出的答案直接就是当前情况下的答案。
>	
>	2.不选择$nums[i]$，那么这时在下一次传入函数的时候，$indx$不需要改动，并且后面计算出来的答案直接就是当前情况下的答案。

综上所述，$dfs(i, indx)$的计算就是以下公式：
$$dfs(i, indx) = 
\begin{cases}
\begin{cases}
dfs(i+1, indx+1)+1 & if\ \ nums[i]==indx-1\\
dfs(i+1, indx+1) & if\ \ nums[i]\ne indx-1
\end{cases}
&(选择nums[i])\\
dfs(i+1, indx) & (不选nums[i])
\end{cases}$$

因此可以很简单写出$dfs$的代码如下：
```python
def dfs(i, indx):
	if i == n:
		return 0
	return max(dfs(i+1, indx), dfs(i+1, indx+1)+(nums[i]==indx))
```

但很可惜，由于其时间复杂度太高，这份简洁优雅的代码并不能通过。

#### LIS(最长递增子序列)优化
再次分析题目要求，如果要将某个位置变成固定点，应当如何操作？可以发现，假设已知$nums[i]=x$，如果要将$nums[i]$变成一个固定点，那么它必须位于数组中索引为$x$的位置，而在本题目中只能通过一个位置之前的数字将该数字向左移动，因此如果要将其变成固定点，在它之前必须删除$i-x$个数字。
（可以发现，这个操作必须在$i\geq x$的时候完成，也就是说只有$i\geq nums[i]$的位置才可能变成固定点）
**（为方便叙述，将每个位置对应的$i-x$值定义为位置$i$的偏移量）**

接下来，如果多个数字要同时以固定点的身份出现在一个子序列中，那么这些数字又应该满足什么条件呢？首先它们的值必须单增，其次，它们对应的偏移量的值必须非递减。

前者容易理解，那后者是为什么呢？可以发现，如果将一个位置的左侧一些值删除，那么它右侧的所有位置都会相应左移同样的距离，因此，如果要让后面的值也成为固定点，那么后面的值「需要左移的距离」必须大于前面的值「需要左移的距离」，也就是它们对应的偏移量的值不能递减。

那么问题就转换成了：先将所有可能变成固定点的位置$i$按照二元组$(nums[i], i-nums[i])$的形式存入一个列表$pairs$中，在$pairs$中能够找到的「第0个位置单增、第1个位置非单减」的最大序列长度。

注意到这里需要满足两个有序条件，因此可以先将所有元素按照其中一个条件进行排序，然后再按顺序查找序列中满足另一个条件的最长子序列。在本题中，本解法选择先按照第1个条件排序，再按顺序查找序列中满足第2个条件的最大序列长度。

定义$dp$数组，其中$dp[i]$表示：选择$i$个固定点时，序列结尾固定点的最小偏移量。当遇到一个新的可能固定点对应的偏移量$d$时，可以在$dp$数组中找到第一个大于$d$的位置$indx$，此时有两种情况：

>	1.$indx=len(dp)$，即当前偏移量是最大的，那么说明此时可以增加所选固定点数量，因此直接将$d$加入$dp$数组中即可；
>	2.$indx < len(dp)$，即存在$dp[indx]>d$，此时即表明发现了选择$indx$个固定点时的一个更小的结尾对应的偏移量，此时为了之后能选择更多固定点，即需将$dp[indx]$更新为$d$。

最后，$dp$中有多少个数字，即说明可以选择多少个满足要求的固定点。

### 实现细节
在对$pairs$数组进行排序的时候，不仅需要按照「第0个位置单增」排序，还需要按照「第1个位置递减」进行排序。这是因为：如果$nums$中有多个相同的元素，它们对应的偏移量一定不同，但由于在后面计算最大序列长度时是按照第1个位置**非递减**的顺序查找的，因此，只有这里提前按照「第1个位置递减」进行排序，才能防止后面对同一个值进行多次计算。

在$dp$数组中找第一个大于$d$的位置时，由于计算过程保证数组单调性，因此可以用二分查找函数来寻找。

### 复杂度
时间复杂度：$O(nlogn)$
空间复杂度：$O(n)$

### 代码
```python
class Solution:
    def maxFixedPoints(self, nums: list[int]) -> int:
        n = len(nums)
        pairs = []
        for i in range(n):
            if i >= nums[i] and nums[i] < n:
                pairs.append((nums[i], i-nums[i]))
        pairs.sort(key = lambda x:(x[0], -x[1]))

        dp = []
        for _, d in pairs:
            indx = bisect_right(dp, d)
            if indx == len(dp):
                dp.append(d)
            else:
                dp[indx] = d
        return len(dp)
```