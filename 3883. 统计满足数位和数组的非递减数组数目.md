### 题目分析
根据题目要求，需要找到每一个给出的数位和对应的$0\sim 5000$中的所有数字，然后找到所有可能的排列中非递减的所有情况数量。由于数字量不大，但在函数内处理会浪费时间，因此可以选择在函数外直接预处理出每一个可能的数位和对应的所有数字，用字典`cnt`存放。

### 基本思路
由于题目要求非递减，同时要求每个位置数字的数位和跟给出的`digitSum`对应位置的数字相等，一种方式是按照各个位置限定的数位和在`cnt`中取出该位置所有可能的数字，然后用`dfs`函数从头到尾统计非递减的总情况数量，但这样太慢，因此需要优化这种方式。

注意到如果对于每一个`i`都将`digitSum[i]`所对应的`cnt`中的每一个值`x`都按照二元组`(x, i)`的方式存入一个列表`tot`中，然后按照从小到大的顺序排序，这样只需在这个列表中按顺序选择数字，即可保证得出的数组一定是非递减的。只不过选出的数对应当保证：相邻两个数对中，后面一个数对的第2个元素恰好比前面一个数对的第2个元素多1，同时需要选出总共`len(digitSum)`个数字。

(记$n = len(digitSum)$，$m=len(tot)$)
因此，如果只保留`tot`中各个二元组的第2个元素，那么此时`tot`中实际上只剩下了$m$个`digitSum`中的索引，而所选的数组实际上就是：$[0,1,2,\dots,n-2,n-1]$这样的索引数组，只有这样，所选位置原先对应的数字才能恰好构成一个：1.非递减，2.每个位置上数字的数位和与给出值相等。

至此，整个题目就变成了：在最终的`tot`数组中统计长度为$n$的连续单增子序列的数量，即统计$[0,1,2,\dots,n-2,n-1]$这样的子序列的数量。

### DFS思路


### DP思路

### 前缀和优化

### 实现细节

### 复杂度

### 代码

```python
cnt = defaultdict(list)
for i in range(5001):
    s = sum(map(int, list(str(i))))
    cnt[s].append(i)

class Solution:
    def countArrays(self, digitSum: list[int]) -> int:
        modNum = 1_000_000_007
        n = len(digitSum)
        tot = []
        for i in range(n):
            for v in cnt[digitSum[i]]:
                tot.append((v, i))
        tot.sort()

        if len(tot) == 0:
            return 0
        tot = [i[1] for i in tot]
        m = len(tot)

        dp = [0]*(m)
        preSum = [0]*(n+1)
        for i in range(m):
            if tot[i] > 0:
                dp[i] = (dp[i]+preSum[tot[i]-1])%modNum
            else:
                dp[i] = 1
            preSum[tot[i]] = (preSum[tot[i]]+dp[i])%modNum
        return preSum[n-1]
```